# =============================================================================
# Mosquitto ACL (Access Control List)
# MING Stack - 산업용 IoT 플랫폼
# =============================================================================
#
# 형식:
#   user <username>
#   topic [read|write|readwrite|deny] <topic>
#
# 패턴:
#   %c - Client ID
#   %u - Username
#   + - 단일 레벨 와일드카드
#   # - 다중 레벨 와일드카드
# =============================================================================

# -----------------------------------------------------------------------------
# 관리자 (모든 토픽 접근 가능)
# -----------------------------------------------------------------------------
user mqtt_user
topic readwrite #

# -----------------------------------------------------------------------------
# Node-RED (모든 토픽 읽기/쓰기)
# -----------------------------------------------------------------------------
user nodered
topic readwrite #

# -----------------------------------------------------------------------------
# 센서 디바이스 (자신의 토픽만 쓰기, 제어 토픽은 읽기)
# -----------------------------------------------------------------------------
user sensor_device
# 센서 데이터 발행
topic write factory/+/+/data
topic write factory/+/+/status
# 제어 명령 수신
topic read factory/+/+/control
topic read factory/+/+/config

# -----------------------------------------------------------------------------
# 익명 사용자 (시스템 토픽만 읽기 - 비활성화 상태)
# -----------------------------------------------------------------------------
# pattern read $SYS/#

# -----------------------------------------------------------------------------
# 공통 패턴 (모든 인증된 사용자)
# -----------------------------------------------------------------------------
pattern read $SYS/#
pattern readwrite factory/%u/#
